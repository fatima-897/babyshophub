import 'package:flutter/material.dart';

/// Central place for app colors.
///
/// Some screens import `../utils/colors.dart` but this file was missing in the repo.
/// Keeping it minimal avoids breaking existing imports.
class AppColors {
  static const Color background = Color(0xFFFAF8F5);
  static const Color primary = Color(0xFF6E5442);
  static const Color accent = Color(0xFF9E826C);
}

